<UserControl x:Class="ARK.Protokolsystem.Pages.BeginTripBoats"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             xmlns:VM="clr-namespace:ARK.ViewModel"
             d:DesignWidth="640" d:DesignHeight="480" Width="600" Height="600">
    <UserControl.DataContext>
        <VM:BeginTripViewModel>
        </VM:BeginTripViewModel>
    </UserControl.DataContext>
    <Grid Width="600" Height="600">
        <Button x:Name="BeginTripNow" Content="PÅBEGYND &#xA;ROTUR" HorizontalAlignment="Left" VerticalAlignment="Top" Width="158" Effect="{Binding Effect, ElementName=EndTrip}" FontSize="16" Style="{StaticResource SmallShadow}" BorderThickness="2" Margin="442,510,0,0" Height="64"  Foreground="White" FontFamily="/Protokolsystem;component/Fonts/#Arial" HorizontalContentAlignment="Left" VerticalContentAlignment="Top" Padding="10,10,1,1" FontWeight="Bold" Command="{Binding Path=StartTripNow}">
            <Button.BorderBrush>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FFB0D141" Offset="0"/>
                    <GradientStop Color="#FF1B4E09" Offset="1"/>
                </LinearGradientBrush>
            </Button.BorderBrush>
            <Button.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF93C700" Offset="0"/>
                    <GradientStop Color="#FF0E6E00" Offset="1"/>
                </LinearGradientBrush>
            </Button.Background>
        </Button>
        <ListView x:Name="AllBoatsList" ItemsSource="{Binding Path=Boats}" HorizontalAlignment="Left" Height="446" VerticalAlignment="Top" Width="295" Opacity="0.975" Margin="0,49,0,0"  SelectionMode="Single" FontSize="18" BorderBrush="#FFB9B9B9">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <WrapPanel Orientation="Vertical">
                        <TextBlock Text="{Binding Name}"></TextBlock>
                        <WrapPanel>
                            <TextBlock Text="Pladser: " FontSize="12px"></TextBlock>
                            <TextBlock Text="{Binding NumberofSeats}" FontSize="12px"></TextBlock>
                        </WrapPanel>
                    </WrapPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction Command="{Binding Path=SelectedChange}">
                    </i:InvokeCommandAction>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ListView>
        <ListView x:Name="AllMembersList" ItemsSource="{Binding Path=Members}" HorizontalAlignment="Left" Height="446" VerticalAlignment="Top" Width="295" Opacity="0.975" Margin="305,49,0,0"  SelectionMode="Multiple" FontSize="18" BorderBrush="#FFB9B9B9" IsEnabled="{Binding Path=EnableMembers}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <TextBlock Text="{Binding FirstName}"></TextBlock>
                        <TextBlock Text="{Binding LastName}"></TextBlock>
                    </WrapPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Label Content="Vælg båd" HorizontalAlignment="Left" Margin="0,14,0,0" VerticalAlignment="Top" Height="40" Width="295" FontSize="20" Foreground="#FF3C3C3C" FontWeight="Bold" />
        <Label Content="Vælg medlemmer" HorizontalAlignment="Left" Margin="305,14,0,0" VerticalAlignment="Top" Height="39" Width="295" FontSize="20" Foreground="#FF3C3C3C" FontWeight="Bold" />
    </Grid>
</UserControl>
