<UserControl x:Class="ARK.View.Administrationssystem.Pages.FormsDamage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:VM="clr-namespace:ARK.ViewModel.Administrationssystem"
             xmlns:valueConverters="clr-namespace:ARK.View.ValueConverters"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="600">
    <UserControl.DataContext>
        <VM:FormsDamageViewModel></VM:FormsDamageViewModel>
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontStyle" Value="Italic"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontStyle" Value="Normal"/>
        </Style>
        <valueConverters:BoolToStringConverter x:Key="Funktionel" FalseValue="Nej" TrueValue="Ja" />
    </UserControl.Resources>
    <StackPanel>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" FontSize="20" HorizontalAlignment="Center" Margin="4" Content="Skadesblanket"/>


            <WrapPanel Grid.Row="1" Margin="4" HorizontalAlignment="Center">

                <StackPanel Width="400" Margin="4">
                    <WrapPanel>
                        <TextBlock Text="Anmelder" Width="100"/>
                        <TextBlock Text="{Binding Path=DamageForm.RegisteringMember.FirstName}"/>
                        <TextBlock Text=" "/>
                        <TextBlock Text="{Binding Path=DamageForm.RegisteringMember.LastName}"/>
                    </WrapPanel>
                    <WrapPanel>
                        <TextBlock Text="Båd" Width="100"/>
                        <TextBlock Text="{Binding Path=DamageForm.Boat.Name}"/>
                    </WrapPanel>
                    <WrapPanel>
                        <TextBlock Text="Indsendt" Width="100"/>
                        <TextBlock Text="{Binding Path=DamageForm.Date, StringFormat=d}"/>
                    </WrapPanel>
                    <WrapPanel>
                        <TextBlock Text="Skadetype" Width="100"/>
                        <TextBlock Text="{Binding Path=DamageForm.Type}"/>
                    </WrapPanel>
                    <WrapPanel>
                        <TextBlock Text="Funktionalitet" Width="100"/>
                        <TextBlock/>
                    </WrapPanel>
                    <WrapPanel>
                        <TextBlock Text="Status" Width="100"/>
                        <TextBlock>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Path=DamageForm.Closed, UpdateSourceTrigger=PropertyChanged}" Value="true">
                                            <Setter Property="Text" Value="Lukket"/>
                                            <Setter Property="Foreground" Value="Red"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </WrapPanel>
                </StackPanel>

                <StackPanel Width="400" Margin="4">
                    <Label Content="Kommentarer"/>
                    <WrapPanel Width="250" HorizontalAlignment="Left">
                        <TextBox AcceptsReturn="True" Height="130" Name="CommentTextbox" Width="250" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Text="{Binding Path=DamageForm.Description, Mode=TwoWay}"/>
                        <Button Content="Gem ændringer" Width="100" Command="{Binding SaveChanges}"/>
                    </WrapPanel>
                </StackPanel>

            </WrapPanel>

            <WrapPanel Grid.Row="2" Margin="4" HorizontalAlignment="Center">
                <Button Background="Transparent" BorderBrush="Transparent" Command="{Binding Path=DeactivateBåd}">
                    <StackPanel Margin="4">
                        <WrapPanel HorizontalAlignment="Center">
                            <Image Source="../../images/application_side_list_red.png" Height="32"/>
                            <Image Source="../../images/add.png" Height="32"/>
                        </WrapPanel>
                        <Label Content="Genopret skadesblanket"/>
                    </StackPanel>
                </Button>
                <Button Background="Transparent" BorderBrush="Transparent" Command="{Binding Path=AktiverBåd}">
                    <StackPanel Margin="4">
                        <WrapPanel HorizontalAlignment="Center">
                            <Image Source="../../images/application_side_list_red.png" Height="32"/>
                            <Image Source="../../images/delete.png" Height="32"/>
                        </WrapPanel>
                        <Label Content="Deaktiver skadesblanket"/>
                    </StackPanel>
                </Button>
            </WrapPanel>
            
            
            <TextBlock Grid.Row="3" FontSize="20">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Path=RecentChange, UpdateSourceTrigger=PropertyChanged}" Value="true">
                                <Setter Property="Text" Value="Ændringer gemt"/>
                                <Setter Property="Foreground" Value="Green"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            
            
        </Grid>
    </StackPanel>
</UserControl>
