<UserControl x:Class="ARK.Administrationssystem.Pages.FormsDamage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:VM="clr-namespace:ARK.ViewModel.Administrationssystem"
             xmlns:valueConverters="clr-namespace:ARK.View.ValueConverters"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <UserControl.DataContext>
        <VM:FormsDamageViewModel></VM:FormsDamageViewModel>
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontStyle" Value="Italic"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontStyle" Value="Normal"/>
        </Style>
        <valueConverters:BoolToStringConverter x:Key="Funktionel" FalseValue="Nej" TrueValue="Ja" />
    </UserControl.Resources>
    <StackPanel>
        <Label FontSize="30" HorizontalAlignment="Left" FontFamily="arial" FontWeight="Bold" Margin="20,0,0,20">Skadesblanket</Label>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            
            
            <Grid Margin="0,0,0,0" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>
                
                <Label Grid.Row="0">Andmelder:</Label>
                <Label Grid.Row="1">Båd:</Label>
                <Label Grid.Row="2">Indsendt:</Label>
                <Label Grid.Row="3">Skade:</Label>
                <Label Grid.Row="4">Funktionel:</Label>

                <WrapPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Margin="0,0,3,0" Text="{Binding Path=DamageForm.RegisteringMember.FirstName}" />
                    <TextBlock Text="{Binding Path=DamageForm.RegisteringMember.LastName}" />
                </WrapPanel>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Path=DamageForm.Boat.Name}" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Path=DamageForm.Date, StringFormat=d}" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Path=DamageForm.Type}" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Path=DamageForm.Functional, Converter={StaticResource Funktionel}}" VerticalAlignment="Center"/>
            </Grid>
            <Grid Grid.Column="1" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="20"/>
                </Grid.RowDefinitions>
                
                <DockPanel DockPanel.Dock="Top">
                    <Label DockPanel.Dock="Top" Content="Kommentarer"/>
                    <TextBox Name="CommentTextbox"  DockPanel.Dock="Bottom" Width="200" FontSize="16" TextWrapping="Wrap" Text="{Binding Path=DamageForm.Description, Mode=TwoWay}">
                        
                    </TextBox>
                </DockPanel>
                <Button Grid.Row="1" Content="Gem ændringer" Command="{Binding SaveChanges}"/>
            </Grid>
                
            <DockPanel Grid.ColumnSpan="2" Grid.Row="2">
                <Button Padding="5" Background="Transparent" BorderBrush="Transparent" Command="{Binding Path=DeactivateBåd}">
                    <StackPanel>
                        <WrapPanel HorizontalAlignment="Center">
                            <Image Source="../../images/yacht.png" Height="24"/>
                            <Image Source="../../images/cross.png" Height="24"/>
                        </WrapPanel>
                        <Label Content="Sæt båden til skadet" FontSize="14" FontStyle="Normal"/>
                    </StackPanel>
                </Button>
                <Button HorizontalAlignment="Left" Padding="5" Background="Transparent" BorderBrush="Transparent" Command="{Binding Path=AktiverBåd}">
                    <StackPanel Margin="0">
                        <WrapPanel HorizontalAlignment="Center">
                            <Image Source="../../images/yacht.png" Height="24"/>
                            <Image Source="../../images/accept.png" Height="24"/>
                        </WrapPanel>
                        <Label Content="Sæt båden til aktiv" FontSize="14" FontStyle="Normal"/>
                    </StackPanel>
                </Button>
                <Button Padding="5" Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Right" Command="{Binding Path=CloseForm}">
                    <StackPanel>
                        <WrapPanel HorizontalAlignment="Center">
                            <Image Source="../../images/cross.png" Height="24"/>
                        </WrapPanel>
                        <Label Content="Luk skadesform" FontSize="14" FontStyle="Normal"/>
                    </StackPanel>
                </Button>
            </DockPanel>
            
            
            <TextBlock Grid.Row="3" FontSize="20">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Path=RecentChange, UpdateSourceTrigger=PropertyChanged}" Value="true">
                                <Setter Property="Text" Value="Ændringer gemt"/>
                                <Setter Property="Foreground" Value="Green"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <TextBlock Grid.Row="4" FontSize="20">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Path=DamageForm.Closed, UpdateSourceTrigger=PropertyChanged}" Value="true">
                                <Setter Property="Text" Value="Denne skadesform er lukket"/>
                                <Setter Property="Foreground" Value="Red"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            
        </Grid>
    </StackPanel>
</UserControl>
