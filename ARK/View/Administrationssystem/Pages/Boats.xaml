<UserControl x:Class="ARK.Administrationssystem.Pages.Baede"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:VM="clr-namespace:ARK.ViewModel.Administrationssystem"
             xmlns:valueConverters="clr-namespace:ARK.View.ValueConverters"
             mc:Ignorable="d" 
             d:DesignHeight="594.417" d:DesignWidth="1121.053">
    <UserControl.DataContext>
        <VM:BoatViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style x:Key="DetailLabels" TargetType="Label">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontStyle" Value="Italic"/>
        </Style>
        <valueConverters:BoolToStringConverter x:Key="YesNo" FalseValue="Nej" TrueValue="Ja" />
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="White" Offset="0.118"/>
                <GradientStop Color="White" Offset="1"/>
                <GradientStop Color="#FFF9F7F7" Offset="0.056"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="5"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" MinWidth="70" MaxWidth="250"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="809*"/>
        </Grid.ColumnDefinitions>
        <WrapPanel Margin="5,0,0,0" Grid.ColumnSpan="3">
            <Image Height="32" Source="/View/images/yacht.png"/>
            <Label FontSize="28" >BÅDE</Label>
        </WrapPanel>
        <DockPanel Grid.Row="2" >
            <Label DockPanel.Dock="Top" Margin="20,0,0,0" FontSize="18">Både</Label>
            <Grid Margin="0,0,5,5">
                <Border BorderBrush="Gray" BorderThickness="1">
                    <ListView x:Name="BoatList" ItemsSource="{Binding Path=Boats}" IsSynchronizedWithCurrentItem="True" SelectionMode="Single">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Width="210">
                                    <StackPanel Margin="5,0,15,0">
                                        <TextBlock Text="{Binding Name}" FontSize="18"/>
                                        <WrapPanel>
                                            <TextBlock Margin="0,0,2,0" Text="Mest anvendende:" FontSize="12"/>
                                            <TextBlock Text="Ubundet" FontSize="12"/>
                                        </WrapPanel>
                                    </StackPanel>

                                    <Grid.Style>
                                        <!-- Gør listview item rød hvis båden er inaktiv-->
                                        <Style TargetType="Grid">
                                            <Setter Property="Background" Value="#CCFFCC"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Path=Active}" Value="False">
                                                    <Setter Property="Background" Value="#FFDEDE"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding Path=SelectedChange}" CommandParameter="{Binding ElementName=BoatList, Path=SelectedItem}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </ListView>
                </Border>
            </Grid>
        </DockPanel>
        <DockPanel Grid.Row="2" Grid.Column="2" >
            <Label DockPanel.Dock="Top" Margin="20,0,0,0" FontSize="18">Detaljer</Label>
            <Border BorderBrush="Gray" BorderThickness="1">
                <Grid Margin="25" Background="White" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="308"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <Grid.Resources>
                            <Style BasedOn="{StaticResource DetailLabels}" TargetType="{x:Type Label}"/>
                        </Grid.Resources>

                        <Label Grid.Row="0" Content="Navn:" />
                        <Label Grid.Row="1" Content="Pladser:"/>
                        <Label Grid.Row="2" Content="Kategori:"/>
                        <Label Grid.Row="3" Content="Aktiv:"/>
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Column="1" FontSize="16" Text="{Binding Path=CurrentBoat.Name}"/>
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Row="1" Grid.Column="1" FontSize="16" Text="{Binding Path=CurrentBoat.NumberofSeats}"/>
                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Row="2" Grid.Column="1" FontSize="16" Text="{Binding Path=CurrentBoat.SpecificBoatType}"/>
                        <CheckBox Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" HorizontalContentAlignment="Center" x:Name="AktivBox" IsChecked="{Binding Path=LocalActiveBoat, Mode=TwoWay}"/>
                        <Button Grid.Row="5" Grid.Column="0" FontSize="20" Padding="8,3,8,3" Margin="5" HorizontalAlignment="Left" Content="Ok" Command="{Binding Path=SaveChanges}" Background="#CCFFCC"/>
                        <Button Grid.Row="5" Grid.Column="1" FontSize="20" Padding="8,3,8,3" Margin="5" HorizontalAlignment="Right" Content="Annuller" Command="{Binding Path=CancelChanges}" Background="#FFDEDE"/>

                        <WrapPanel Grid.Row="6" Grid.ColumnSpan="2" Margin="10" HorizontalAlignment="Center">
                            <Image Height="24">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding RecentSave, UpdateSourceTrigger=PropertyChanged}" Value="true">
                                                <Setter Property="Source" Value="../../images/accept.png"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding RecentCancel, UpdateSourceTrigger=PropertyChanged}" Value="true">
                                                <Setter Property="Source" Value="../../images/cross.png"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <TextBlock FontSize="16" Margin="8,0,0,0" VerticalAlignment="Center" >
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=RecentSave, UpdateSourceTrigger=PropertyChanged}" Value="true">
                                                <Setter Property="Foreground" Value="Green"/>
                                                <Setter Property="Text" Value="Ændringer gemt"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=RecentCancel, UpdateSourceTrigger=PropertyChanged}" Value="true">
                                                <Setter Property="Foreground" Value="Red"/>
                                                <Setter Property="Text" Value="Ændringer annulleret"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </WrapPanel>

                    </Grid>
                    <Grid Grid.Row="2" Grid.ColumnSpan="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>


                        <Border Style="{DynamicResource ResourceKey=brdHeadline}" Width="270" Background="AliceBlue" Grid.Row="0">
                            <WrapPanel Margin="0,2,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="20"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Image Source="/View/images/user.png" Grid.Column="0" Height="16" Width="16" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                    <WrapPanel Grid.Column="1">
                                        <TextBlock Text="Ture (" Foreground="Black" FontSize="18"/>
                                        <TextBlock Text="{Binding Path=CurrentBoat.Trips.Count}" Foreground="Black" FontSize="18"/>
                                        <TextBlock Text=")" Foreground="Black" FontSize="18"/>
                                    </WrapPanel>
                                </Grid>
                            </WrapPanel>
                        </Border>

                        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="1" PanningMode="VerticalOnly">
                            <ItemsControl  x:Name="BoatTripsList" ItemsSource="{Binding Path=CurrentBoat.Trips}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="95"/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Margin="5,0,0,0" Grid.Column="0">
                                                <WrapPanel>
                                                    <!--<TextBlock Text="Retning "/>-->
                                                    <TextBlock Text="{Binding Path=Direction}"/>
                                                </WrapPanel>
                                                <WrapPanel>
                                                    <TextBlock Text="{Binding Path=Distance}"/>
                                                    <TextBlock Text=" Km"/>
                                                </WrapPanel>
                                            </StackPanel>
                                            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Column="1">
                                                <WrapPanel>
                                                    <TextBlock Text="Start:  "/>
                                                    <TextBlock Text="{Binding Path=TripStartTime, ConverterCulture='da-DK'}"/>
                                                </WrapPanel>
                                                <WrapPanel>
                                                    <TextBlock Text="Slut:   "/>
                                                    <TextBlock Text="{Binding Path=TripEndedTime, ConverterCulture='da-DK'}"/>
                                                </WrapPanel>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Border>
        </DockPanel>



        <GridSplitter HorizontalAlignment="Right" Margin="0,35,0,0"
			VerticalAlignment="Stretch" 
			Grid.Column="1" Grid.Row="2" ResizeBehavior="PreviousAndNext"
			Width="5" Background="Gray"/>
    </Grid>
    
</UserControl>
