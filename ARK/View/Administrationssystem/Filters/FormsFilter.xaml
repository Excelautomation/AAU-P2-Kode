<UserControl x:Class="ARK.View.Administrationssystem.Filters.FormsFilter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:VMFilter="clr-namespace:ARK.ViewModel.Administrationssystem.Filters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <VMFilter:FormsFilterViewModel></VMFilter:FormsFilterViewModel>
    </UserControl.DataContext>
    
    <StackPanel>
        <!--<CheckBox Content="Langtur" IsChecked="{Binding Path=ShowLongDistanceForm, Mode=TwoWay}"></CheckBox>
        <CheckBox Content="Skader" IsChecked="{Binding Path=ShowDamageTypes, Mode=TwoWay}"></CheckBox>
        <Separator Height="20"></Separator>-->
        <CheckBox Content="Afventer" IsChecked="{Binding Path=ShowOpen, Mode=TwoWay}"></CheckBox>
        <CheckBox Content="Afviste" IsChecked="{Binding Path=ShowDenied, Mode=TwoWay}"></CheckBox>
        <CheckBox Content="Godkendte" IsChecked="{Binding Path=ShowAccepted, Mode=TwoWay}"></CheckBox>
    </StackPanel>
</UserControl>
